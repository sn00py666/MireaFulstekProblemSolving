#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int t;
    
    cin >> t;
    
    cout << t / 3600 << ":" << (t % 3600) / 600 << (t % 3600) / 60 % 10 << ":" << t % 60 / 10 << t % 60 % 10;

                            
    return 0;
}