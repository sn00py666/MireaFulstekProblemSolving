#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int a;
    int b;
    double c;
    
    cin >> a;
    cin >> b;
    c = sqrt(a*a + b*b);
    
    cout << c << std::endl;
                            
    return 0;
}